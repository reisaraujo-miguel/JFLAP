app-id: io.github.reisaraujo_miguel.jflap-ng
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
command: jflap-ng

sdk-extensions:
    - org.freedesktop.Sdk.Extension.openjdk25

finish-args:
  # XDG Desktop Portals for file access
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --device=dri
  # File system access
  - --filesystem=xdg-documents
  - --filesystem=xdg-download
  - --filesystem=xdg-pictures
  # XDG portals for file dialogs
  - --talk-name=org.freedesktop.FileManager1
  - --env=PATH=/app/jre/bin:/usr/bin:/app/bin:$PATH
  - --env=JAVA_HOME=/app/jre

modules:
  - name: openjdk
    buildsystem: simple
    build-commands:
      - /usr/lib/sdk/openjdk25/install.sh

  - name: jflap-ng
    buildsystem: simple
    build-options:
      env:
        PATH: /app/bin:/usr/bin:/usr/lib/sdk/openjdk25/bin
        JAVA_HOME: /usr/lib/sdk/openjdk25/jvm/openjdk-25
        GRADLE_OPTS: -Dorg.gradle.offline=true
    build-commands:
      # unzip and use the pre downloaded Gradle distribution
      - unzip -q gradle-9.1.0-bin.zip
      - ./gradle-9.1.0/bin/gradle build fatJar
      # Create the application directory structure
      - install -Dm0644 build/libs/JFLAP-NG_Fat-fat.jar /app/share/jflap-ng.jar
      - install -Dm755 jflap-ng /app/bin/jflap-ng
      - install -Dm0644 flatpak/${FLATPAK_ID}.desktop /app/share/applications/${FLATPAK_ID}.desktop
      - install -Dm0644 flatpak/${FLATPAK_ID}.metainfo.xml /app/share/metainfo/${FLATPAK_ID}.metainfo.xml
      - install -Dm0644 DOCS/images/${FLATPAK_ID}.svg /app/share/icons/hicolor/scalable/apps/${FLATPAK_ID}.svg
    post-install:
      - install -Dm0644 LICENSE -t /app/share/licenses/${FLATPAK_ID}/jflap-ng

    sources:
      - type: git
        url: https://github.com/reisaraujo-miguel/jflap-ng.git
        branch: main

      - type: script
        dest-filename: jflap-ng
        commands:
          - java -jar /app/share/jflap-ng.jar

      - type: file
        url: https://services.gradle.org/distributions/gradle-9.1.0-bin.zip
        sha256: a17ddd85a26b6a7f5ddb71ff8b05fc5104c0202c6e64782429790c933686c806
        dest-filename: gradle-9.1.0-bin.zip

      - flatpak-sources.json


